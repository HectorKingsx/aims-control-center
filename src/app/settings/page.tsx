import Placeholder from "@/components/Placeholder";
export default function Page() {
  return <Placeholder title="Settings" note="Akan menampilkan brand_settings (threshold, daily cap, banned topics) sebagai form editable — Phase 2." />;
}
